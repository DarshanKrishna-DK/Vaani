# Frontend talks to the backend via the /api proxy configured in vite.config.ts.
# No frontend env vars are required for local development.
# If you deploy the frontend separately, set the API base URL here and update lib/api.ts.
# VITE_API_BASE=http://localhost:5050
